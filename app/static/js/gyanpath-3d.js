(()=>{const canvas=document.getElementById("gyanpath-3d");if(!canvas||typeof THREE==="undefined"||matchMedia("(prefers-reduced-motion: reduce)").matches)return;const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(50,innerWidth/innerHeight,.1,100);camera.position.z=6;const renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,1.5));renderer.setSize(innerWidth,innerHeight);const group=new THREE.Group();const geo=new THREE.TorusKnotGeometry(1.35,.055,120,12);const mat=new THREE.MeshBasicMaterial({color:0xeac878,transparent:true,opacity:.38});const mesh=new THREE.Mesh(geo,mat);group.add(mesh);const ring=new THREE.Mesh(new THREE.TorusGeometry(2.0,.012,12,100),new THREE.MeshBasicMaterial({color:0xc96545,transparent:true,opacity:.35}));ring.rotation.x=1.1;group.add(ring);scene.add(group);const onResize=()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)};addEventListener("resize",onResize);const tick=()=>{group.rotation.x+=.0015;group.rotation.y+=.0022;ring.rotation.z-=.001;renderer.render(scene,camera);requestAnimationFrame(tick)};tick()})();